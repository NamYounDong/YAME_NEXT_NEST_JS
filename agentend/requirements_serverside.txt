# ===================================================
# YAME Agentend - 서버 배포용 경량화 requirements
# ===================================================
# 로컬 개발: requirements.txt 사용
# 서버 배포: requirements_serverside.txt 사용 (경량화)
# ===================================================

# --- FastAPI 서버 ---
fastapi
uvicorn[standard]

# --- LangChain / GPT 모델 ---
langchain==0.1.16
langchain-core==0.1.46
langchain-community==0.0.34
langchain-openai==0.0.5   # OpenAI API (Embeddings + LLM)

# --- 환경 변수 ---
python-dotenv

# --- MariaDB 연동 ---
SQLAlchemy==2.0.36
pymysql               # MariaDB 드라이버

# --- 벡터 스토어 (서버에서는 로드만 수행) ---
langchain-chroma      # 벡터 스토어 로드
chromadb              # 벡터 DB 런타임

# --- Redis (세션 관리) ---
redis                 # 대화 히스토리 저장

# --- 설정 관리 ---
pydantic-settings     # 설정 관리

# ===================================================
# 제거된 모듈 (서버 배포에서 불필요)
# ===================================================
# sentence-transformers  → 미사용 (OpenAI API 사용)
# websockets            → 미사용 (Backend에서 처리)
# ===================================================
# 예상 용량 절감: 약 1.5GB
# ===================================================
